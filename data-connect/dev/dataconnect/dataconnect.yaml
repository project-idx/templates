specVersion: "v1alpha"
serviceId: "local"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "emulator"
      cloudSql:
        instanceId: "my-instance"
connectorDirs: ["./connector"]